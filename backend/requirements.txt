fastapi==0.135.3
uvicorn[standard]==0.44.0
pydantic==2.13.1
pydantic[email]==2.13.1
python-multipart==0.0.26
opencv-python-headless==4.13.0.92
numpy==2.4.4
Pillow==12.2.0
EasyOCR==1.7.2
rapidfuzz==3.14.5
metaphone==0.6
requests==2.33.1
# Vision providers (Gemini primary, Claude secondary).
# Default v2.4.0 runtime is VISION_PRIMARY=true; EasyOCR is the fallback
# when the Vision LLM chain fails, so it stays pinned above.
google-genai>=1.73.1,<2
anthropic>=0.95.0,<1
aiohttp==3.13.5
sqlalchemy==2.0.49
orjson==3.11.8
# Authentication & Security
# PyJWT replaces python-jose (CVE-2024-33663 algorithm confusion,
# CVE-2024-33664 JWT decompression bomb).
PyJWT>=2.12.1,<3
cryptography>=46.0.7,<47
passlib[bcrypt]==1.7.4
# Job Processing (Redis only — Celery was removed in v2.4.0 together
# with the orphan backend/app/tasks.py worker).
redis==7.4.0
# Testing
pytest==9.0.3
pytest-asyncio==1.3.0
httpx==0.28.1
# Monitoring
prometheus-client==0.25.0
# OpenTelemetry — the stack stays installed because main.py imports
# telemetry.py unconditionally. Gate by FEATURE_TELEMETRY at runtime.
opentelemetry-api==1.41.0
opentelemetry-sdk==1.41.0
opentelemetry-instrumentation==0.62b0
opentelemetry-instrumentation-fastapi==0.62b0
opentelemetry-instrumentation-redis==0.62b0
opentelemetry-instrumentation-sqlalchemy==0.62b0
opentelemetry-instrumentation-logging==0.62b0
opentelemetry-exporter-otlp==1.41.0
opentelemetry-exporter-otlp-proto-grpc==1.41.0
# Database (sync psycopg only — asyncpg was never imported and
# CLAUDE.md forbids it).
alembic==1.18.4
# Settings
pydantic-settings==2.13.1
python-magic==0.4.27
psutil==7.2.2
