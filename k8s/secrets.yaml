apiVersion: v1
kind: Secret
metadata:
  name: screen2deck-secrets
  namespace: screen2deck
type: Opaque
stringData:
  # These should be replaced with actual values in production
  # Use sealed-secrets or external-secrets-operator for production
  database-url: "postgresql://screen2deck:changeme@postgres:5432/screen2deck"
  redis-url: "redis://redis:6379/0"
  jwt-secret: "your-super-secret-jwt-key-change-in-production"
  api-key-salt: "your-api-key-salt-change-in-production"